services:
  oss-projects:
    build:
      context: .
      platforms:
        - linux/amd64
    image: oss-projects:test
    container_name: oss-projects
    ports:
      - "8080:8080"
    platform: linux/amd64
